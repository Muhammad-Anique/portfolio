export const metadata = {
  title: "Exotic Fleet OS — Notion Workspace Proposal",
  description:
    "Custom Notion workspace proposal for exotic & luxury vehicle rental operations — fleet tracking, ROI calculations, and financial dashboards by Muhammad Anique.",
};

export default function FleetLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <>{children}</>;
}
