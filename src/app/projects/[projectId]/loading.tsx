import React from "react";

const Loading = () => {
  return (
    <div className="w-sceen flex items-center justify-center h-screen">
      <p className="text-zinc-700 dark:text-zinc-200">Loading...</p>
    </div>
  );
};

export default Loading;
